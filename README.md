# questionable-scraper

Search for various sku info across many multi-billion dollar retails stores.

## Running

```clojure
$ lein deps
$ lein run
```

Open http://localhost:3000

### Tests
``` clojure
$ lein test
```
